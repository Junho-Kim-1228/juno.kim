# Django API base URL. Leave empty to use /api/v1 on the current origin.
VITE_API_BASE_URL=
# Local development keeps Django admin at its conventional path. Production injects
# the private admin path during the deploy build.
VITE_ADMIN_URL=/admin/
