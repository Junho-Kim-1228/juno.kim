limit_req_zone $binary_remote_addr zone=juno_admin_login:10m rate=5r/m;
limit_req_zone $binary_remote_addr zone=juno_registration:10m rate=2r/m;
limit_req_zone $binary_remote_addr zone=juno_verification_resend:10m rate=2r/m;

server {
    listen 443 ssl;
    listen [::]:443 ssl ipv6only=on;
    server_name juno.kim 43.200.172.77;

    ssl_certificate /etc/letsencrypt/live/juno.kim/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/juno.kim/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    root /var/www/juno-kim/frontend/dist;
    index index.html;

    access_log /var/log/nginx/juno-kim.access.log;
    error_log /var/log/nginx/juno-kim.error.log warn;

    server_tokens off;
    client_max_body_size 10m;
    limit_req_status 429;
    proxy_hide_header Strict-Transport-Security;
    proxy_hide_header X-Content-Type-Options;
    proxy_hide_header X-Frame-Options;
    proxy_hide_header Referrer-Policy;

    add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; font-src 'self' data: https://cdn.jsdelivr.net; img-src 'self' data:; connect-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'; upgrade-insecure-requests" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=(), usb=()" always;
    add_header Strict-Transport-Security "max-age=31536000" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "same-origin" always;

    location = /api/v1/auth/register/ {
        limit_req zone=juno_registration burst=1 nodelay;
        proxy_pass http://unix:/run/juno-kim/gunicorn.sock;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
    }

    location = /api/v1/auth/resend-verification/ {
        limit_req zone=juno_verification_resend burst=1 nodelay;
        proxy_pass http://unix:/run/juno-kim/gunicorn.sock;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
    }

    location /api/ {
        proxy_pass http://unix:/run/juno-kim/gunicorn.sock;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
    }

    # The conventional Django path is deliberately a harmless prank page.
    # The real admin path is rendered from ADMIN_URL at deploy time.
    location = /admin {
        return 302 /admin/;
    }

    location ^~ /admin/ {
        add_header Cache-Control "no-store" always;
        add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
        try_files /admin/index.html =404;
    }

    location = /__ADMIN_PATH__ {
        return 301 /__ADMIN_PATH__/;
    }

    location = /__ADMIN_PATH__/login/ {
        limit_req zone=juno_admin_login burst=5 nodelay;
        proxy_pass http://unix:/run/juno-kim/gunicorn.sock;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
    }

    location /__ADMIN_PATH__/ {
        proxy_pass http://unix:/run/juno-kim/gunicorn.sock;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
    }

    location /static/ {
        alias /var/www/juno-kim/backend/staticfiles/;
        access_log off;
        expires 7d;
    }

    location /media/ {
        alias /var/www/juno-kim/backend/media/;
        expires 1h;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name juno.kim 43.200.172.77;

    if ($host = juno.kim) {
        return 301 https://$host$request_uri;
    }

    return 404;
}
